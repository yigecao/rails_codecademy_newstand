<div class="signup">
  <div class="container">
    
    <!-- Your form here -->
    <%= form_for(@user) do |u|%>
    <%= u.text_field :first_name, :placeholder => 'First Name'%>
    <%= u.text_field :last_name, :placeholder => 'Last Name'%>
    <%= u.email_field :first_name, :placeholder => 'Email'%>
    <%= u.password_field :password, :placeholder => 'Password'%>
    <%= u.submit 'Sign Up'%>
    <%end%>
    
  </div>
</div>